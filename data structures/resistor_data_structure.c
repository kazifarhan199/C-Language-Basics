// Register data structure It is the fastest data structure (uses registre inside the processor)Limitations can only declare 3 to 4 depending on processor)
#include<stdio.h>

void main()
{
    register int a=0;
    printf("This is the fastest data tructure : Register\n", );
}
